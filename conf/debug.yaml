defaults:
  - contact_params: cg_simple_4_True_0.yaml
  - _self_

hydra:
  run:
    dir: gcs_examples/tmp_outputs/${problem_relative_folder}

problem_relative_folder: shao_large_gcs/outputs/${contact_params.graph_name}_${contact_params.use_l1_cost}_${contact_params.seed}
solve_contact: true
force_remake: true
num_trials: 2
